{
  "name": "session-buddy-mermaid-validator",
  "private": true,
  "description": "Wave-11 CI guard dev deps for the mermaid diagram validator. The runtime mermaid package is loaded from the homebrew mermaid-cli install; jsdom is the only Node dep we add here.",
  "devDependencies": {
    "jsdom": "^26.0.0"
  }
}